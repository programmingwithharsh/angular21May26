name: Claude Review Demo

on:
  pull_request:

jobs:
  review:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Show Changed Files
        run: git diff origin/main...HEAD --name-only

      - name: Claude Review Placeholder
        run: echo "Claude would review code here"